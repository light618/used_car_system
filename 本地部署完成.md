# 本地部署完成

主公，系统已成功部署到本地！

## ✅ 部署状态

✅ **数据库已创建**：`used_car_system`
✅ **数据库表已导入**：5个表（门店、用户、车源、图片、授权）
✅ **默认账号已创建**：admin / admin123
✅ **文件权限已设置**：上传目录可写

## 🌐 访问地址

### 前端管理界面（主要入口）
**http://localhost:8080/admin/index.html**

### API接口
**http://localhost:8080/api/**

## 🔐 默认登录账号

- **用户名**：`admin`
- **密码**：`admin123`

## 💾 数据库信息

- **数据库名**：`used_car_system`
- **主机**：`127.0.0.1`
- **端口**：`3306`
- **用户名**：`root`
- **密码**：空（如果您的MySQL有密码，请修改 `config/database.php`）

## 🚀 启动服务器

### 方法一：使用启动脚本（推荐）
```bash
cd /Users/yiche/used-car-system
bash 启动服务器.sh
```

### 方法二：手动启动
```bash
cd /Users/yiche/used-car-system
php -S localhost:8080 -t public router.php
```

### 方法三：如果PHP不在PATH中
```bash
# 查找PHP路径
which php
# 或
/usr/local/bin/php -S localhost:8080 -t public router.php
```

## 📝 快速测试

1. **启动服务器**（如果还没启动）
   ```bash
   bash 启动服务器.sh
   ```

2. **打开浏览器访问**
   ```
   http://localhost:8080/admin/index.html
   ```

3. **登录系统**
   - 用户名：`admin`
   - 密码：`admin123`

4. **开始使用**
   - 创建门店
   - 录入车源
   - 审核车源
   - 授权车源

## 🛑 停止服务器

如果需要停止PHP服务器，执行：
```bash
pkill -f "php -S localhost:8080"
```

## ⚠️ 注意事项

1. **PHP未安装**：如果提示找不到PHP，请先安装：
   ```bash
   # macOS
   brew install php
   ```

2. **数据库密码**：如果您的MySQL root用户有密码，请修改 `config/database.php` 中的 `password` 字段

3. **端口占用**：如果8080端口被占用，可以修改启动脚本中的端口号

4. **文件权限**：确保 `public/uploads` 目录有写入权限（已设置为777）

## 📂 项目位置

```
/Users/yiche/used-car-system/
```

主公，系统已就绪，可以开始使用了！🎉

